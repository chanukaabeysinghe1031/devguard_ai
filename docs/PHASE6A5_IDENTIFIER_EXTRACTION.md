# Phase 6A.5 — Identifier Extraction

Version: `retrieval_identifier_extractor_v1`

Extracts AWS actions/ARNs/errors, Terraform resources, GHA paths, packages, exceptions, exit codes, changed paths, commit SHAs.

Never persists passwords, tokens, private keys, or credential strings.
